{
  "type": "object",
  "required": ["verdict", "evidence"],
  "additionalProperties": false,
  "properties": {
    "verdict": {"type": "string"},
    "evidence": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["step_id", "category", "explanation"],
        "properties": {
          "step_id": {"type": "integer"},
          "category": {"type": "string", "enum": ["unsupported_claim", "tool_error", "instruction_violation", "insufficient_context"]},
          "explanation": {"type": "string"}
        },
        "additionalProperties": false
      }
    }
  }
}
